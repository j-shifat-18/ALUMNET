// No request-body validation is needed for notification endpoints —
// all inputs come from URL params or query strings, which are handled
// directly in the controller.
//
// This file is kept as a placeholder so the module structure stays
// consistent with the rest of the codebase and additional validation
// can be added later (e.g. bulk-delete by type, filter by type, etc.).

export {};
